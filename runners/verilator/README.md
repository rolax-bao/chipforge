# Verilator runner

Isolated container image that compiles & simulates SV/Verilog designs server-side.

Built: PR #15. This PR only ships the Dockerfile scaffold so the image path exists for Compose and CI smoke tests.
