# @chipforge/ai-core

Shared TypeScript types between the web frontend and any JS/TS tools that need AI-related types (chat messages, streaming events, prompt template names).

Real LLM provider implementations live in `apps/api/chipforge_api/ai/`.
